# === AI ROUTER CONFIGURATION ===
# Key for Clients (Codex/Open WebUI) to Access Router
AI_ROUTER_API_KEY=sk-router-change-me

# Local LLM Provider (Ollama)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_CODER_MODEL=deepseek-coder-v2:16b
OLLAMA_INSTRUCT_MODEL=hermes3:8b

# Fallback Settings
# Set to '1' to allow cloud usage. '0' forces local-only (Default).
# NOTE: config/router_config.yaml (enable_cloud_fallback) MUST be true for this to work.
ENABLE_OPENAI_FALLBACK=0

# (Optional) Cloud Key - Only used if fallback is enabled AND config allows it
OPENAI_API_KEY_TIER2=sk-proj-YOUR_ACTUAL_KEY_HERE
OPENAI_TIMEOUT_SEC=20
